let username = "  Rasty FullStaxx  ";

// .length = computes the length of the string including the whitespace
//console.log(username.length)

// charAt() = access a character from a string through the index
//console.log(username.charAt(6));

// .indexOf() = find the index of a character from a string
//console.log(username.indexOf("F"));
//console.log(username.lastIndexOf("l"));         // last occurence 

// .trim = trims the empty parts of a string
//console.log(username.trim());

// toUpperCase() = upper cases the string
console.log(username.toUpperCase());

// toLowerCase() = lower cases the string
console.log(username.toLowerCase());


//replaceAll() = replaces a character with another. comes with 2 arguments
let phonenumber = "123-456-7890";
phonenumber = phonenumber.replaceAll("-", "/");
console.log(phonenumber)

// THESE ARE ONLY FEW